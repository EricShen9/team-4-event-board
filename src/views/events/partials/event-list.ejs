<div id="event-list">
  <% if (pageError) { %>
    <%- include("../../partials/error", { message: pageError }) %>
  <% } %>

  <% if (events.length === 0) { %>
    <p class="text-slate-500">No events found.</p>
  <% } else { %>
    <div class="space-y-4">
      <% events.forEach(function(event) { %>
        <a href="/events/<%= event.id %>" class="block p-4 border border-slate-200 rounded-xl hover:bg-slate-50 no-underline">
          <div class="flex items-center justify-between mb-1">
            <h2 class="text-lg font-semibold text-slate-900"><%= event.title %></h2>
            <span class="text-xs px-2 py-1 rounded bg-slate-100 text-slate-600"><%= event.category %></span>
          </div>
          <p class="text-sm text-slate-500 mb-1"><%= event.location %> &middot; <%= new Date(event.startDateTime).toLocaleDateString() %></p>
          <p class="text-sm text-slate-600"><%= event.description.substring(0, 100) %><%= event.description.length > 100 ? '...' : '' %></p>
        </a>
      <% }) %>
    </div>
  <% } %>
</div>